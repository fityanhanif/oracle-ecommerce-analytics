# Scripts Folder

This folder is reserved for helper scripts used later in the project.

Possible scripts:

- CSV preprocessing before Oracle import
- row count validation
- SQL output export
- dashboard data export

Version 1 can be completed with Oracle SQL Developer and SQL scripts only, so scripts are optional.
